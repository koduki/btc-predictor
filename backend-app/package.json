{
  "name": "btc-predictor-backend",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "author": "koduki",
  "engines": {
    "node": ">=6.14.0"
  },
  "main": "dist/index.js",
  "scripts": {
    "lint": "samples lint",
    "pretest": "npm run lint",
    "test": "ava -T 20s --verbose test/*.test.js",
    "clean": "rm -fr ./dist/",
    "transpile": "babel src --out-dir dist",
    "deploy": "npm run clean && npm run transpile && gcloud functions deploy helloGET --stage-bucket btcp-cloud-functions-001 --trigger-http"
  },
  "dependencies": {
    "@google-cloud/storage": "1.7.0",
    "nmp": "^1.0.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/node": "^7.0.0",
    "@babel/preset-env": "^7.0.0"
  }
}
